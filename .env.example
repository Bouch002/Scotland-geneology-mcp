# No API keys required — all sources used by this MCP are public.
# Copy this file to .env if you need to override defaults in future.

# Optional: override the NLS WMS base URL if using a mirror
# NLS_WMS_BASE=https://gis.nls.uk/wms/historical

# Optional: override the Archives Hub SRU endpoint
# ARCHIVES_HUB_SRU=https://archiveshub.jisc.ac.uk/sru/
